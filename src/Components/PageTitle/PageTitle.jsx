function PageTitle({ title }) {
  return (
    <div className="bg-primary border border-dark border-1 mb-3 rounded-3">
      <h2 className="text-center text-white fw-bold">{title}</h2>
    </div>
  );
}

export default PageTitle;
